import { Text } from "react-native";
import { Screen } from "../../shared/components/Screen";

export default function AcademyScreen() {
  return (
    <Screen>
      <Text className="text-lg font-semibold p-4">Academia — próximamente</Text>
    </Screen>
  );
}
