import { Text } from "react-native";
import { Screen } from "../../shared/components/Screen";

export default function HistoryScreen() {
  return (
    <Screen>
      <Text className="text-lg font-semibold p-4">Historial — próximamente</Text>
    </Screen>
  );
}
