import { Text } from "react-native";
import { Screen } from "../../shared/components/Screen";

export default function ProfileScreen() {
  return (
    <Screen>
      <Text className="text-lg font-semibold p-4">Perfil — próximamente</Text>
    </Screen>
  );
}
